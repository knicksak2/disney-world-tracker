import React from 'react';
import { Text, View } from 'react-native';

/**
 * Friends screen placeholder.
 *
 * Real implementation lands in a later task; this stub exists so the
 * navigation shell (task 14.3) can be wired and typechecked.
 */
export default function FriendsScreen(): JSX.Element {
  return (
    <View>
      <Text>FriendsScreen</Text>
    </View>
  );
}
