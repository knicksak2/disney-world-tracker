# AGENTS.md

This file is a pointer to the single source of truth at the repository root: [`../AGENTS.md`](../AGENTS.md).

Before doing any work, open the root `AGENTS.md` and follow it in full — it defines the mandatory initial tool-call sequence (read every file in `.kiro/steering/` first), the spec workflow, the communication style, and the verification/enforcement rules. Everything there applies to work started from this directory too.
